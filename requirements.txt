# ============================================
# requirements.txt — Python package dependencies
# ============================================
# Install all of these with:   pip install -r requirements.txt
#
# What each package does:
#   wfdb         — Reads ECG data files in the WFDB (WaveForm DataBase) format
#   pandas       — Loads and manipulates CSV files and tabular data
#   numpy        — Fast numerical operations on arrays (the backbone of scientific Python)
#   scikit-learn — Machine learning library (SVM, Random Forest, K-Means, metrics)
#   scipy        — Scientific computing (signal processing, statistics, FFT)
#   matplotlib   — Creates plots and charts
#   PyWavelets   — Wavelet analysis (decomposes signals into time-frequency layers)
#   torch        — PyTorch deep learning framework (used for the 1D CNN model)
#
# GPU SUPPORT (recommended for CNN training):
#   The default "pip install torch" installs the CPU-only build.
#   For GPU training, first check your CUDA version with "nvidia-smi", then run:
#
#     CPU only (default):
#       pip install torch>=2.0.0
#
#     CUDA 12.1 (most modern NVIDIA GPUs):
#       pip install torch --index-url https://download.pytorch.org/whl/cu121
#
#     CUDA 11.8 (older NVIDIA GPUs):
#       pip install torch --index-url https://download.pytorch.org/whl/cu118
#
#   After installing, verify with:
#     python -c "import torch; print(torch.cuda.is_available())"

wfdb>=4.1.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0
matplotlib>=3.7.0
PyWavelets>=1.4.0
torch>=2.0.0
